name: prebuild-package

# choice boolean environment

on:
  push:
    tags:
      - 'v**'
  workflow_dispatch:
    inputs:
      on-win10:
        description: 'Buidl on windows10'
        required: true
        type: boolean
        default: true
      logLevel:
        description: 'Log level'
        required: true
        default: 'warning'
        type: choice
        options:
        - info
        - warning
        - debug
      tags:
        description: 'Test scenario tags'
        required: false
        type: boolean
      environment:
        description: 'Environment to run tests against'
        type: string
        required: true

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions: write-all

jobs:
  windows-prebuild-package-onnxruntime:
    runs-on: windows-latest
    steps:
      # - name: Install Conda
      #   uses: conda-incubator/setup-miniconda@v2
      # - name: Install python
      #   run: |
      #     conda create -n mmdeploy-3.6 python=3.6
      #     conda create -n mmdeploy-3.7 python=3.7
      #     conda create -n mmdeploy-3.8 python=3.8
      #     conda create -n mmdeploy-3.9 python=3.9
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          submodules: 'recursive'
      - name: Build
        run: |
          ls
          Compress-Archive -Path csrc -DestinationPath win.zip
          ls
      - name: Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/v')
        with:
          files: |
            win.zip

  ubuntu20-04-prebuild-package:
    runs-on: ubuntu-20.04
    container: nvidia/cuda:11.3.0-cudnn8-devel-ubuntu20.04
    steps:
      - name: Echo
        run: |
          echo "hello"
      - name: DABAO
        run: |
          touch mmdeploy
          tar cf mmdeploy.tar.gz mmdeploy
      - name: Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/v')
        with:
          files: |
            mmdeploy.tar.gz